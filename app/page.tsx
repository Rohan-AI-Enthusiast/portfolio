import { Nav } from "@/components/site/Nav";
import { ScrollFrameScene } from "@/components/site/ScrollFrameScene";

export default function Page() {
  return (
    <main className="relative">
      <Nav />
      <ScrollFrameScene />
    </main>
  );
}
